# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

data/test_output/*.html

airflow/config/airflow.cfg


# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Pytest / test caches
.pytest_cache/
.cache/
.mypy_cache/
.ruff_cache/

# Coverage
.coverage
.coverage.*
coverage.xml
htmlcov/


# Jupyter
.ipynb_checkpoints/

# Environment variables and local config
.env
.env.local
.env.*.local
*.local

# Data directories (ingest / processed outputs)
# These can be large; tracked only if intentionally committed.
data/raw/
data/processed/
data/demo/
data/test_output/
data/postgres/  # PostgreSQL database files (nếu mount trực tiếp)
backups/postgres/*.dump  # PostgreSQL backup files
backups/postgres/*.sql
backups/postgres/*.tar
!backups/postgres/.gitkeep
# Note: .github/ should be committed (contains workflows, templates, etc.)

# Logs and runtime files
logs/
*.log

# Airflow / Docker runtime
airflow/logs/
airflow/airflow.cfg
docker-compose.override.yml
docker-compose.*.yml
# Keep separate-db version for reference
!docker-compose.separate-db.yaml

# Docker volumes (data persistence)
postgres-shared-volume/
postgres-db-volume/
nuq-postgres-volume/
*.sql
*.dump
*.backup

# Secrets and credentials
*.pem
*.key
secrets/
credentials/


# IDEs and editors
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# OS files
.DS_Store
Thumbs.db

# Additional cache and build files (already covered above, but explicitly listed)
# Cache directories
.pytest_cache/
.mypy_cache/
.cache/
.ruff_cache/

# Coverage files
.coverage.*
htmlcov/

# Log files
*.log

# Environment files (already covered, but explicit)
.env.local
.env.*.local
