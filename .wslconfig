# WSL Configuration để giới hạn tài nguyên
# File này nên được đặt ở: C:\Users\<YourUsername>\.wslconfig
# Sau khi tạo/sửa file, chạy: wsl --shutdown (trong PowerShell/CMD với quyền admin)

[wsl2]
# Giới hạn memory cho WSL (4GB thay vì mặc định ~50% RAM)
memory=4GB

# Giới hạn số CPU cores (50% số cores)
processors=4

# Giới hạn swap (2GB)
swap=2GB

# Tắt page reporting để giảm overhead
pageReporting=false

# Tắt nested virtualization nếu không cần
nestedVirtualization=false

# Localhost forwarding
localhostForwarding=true

