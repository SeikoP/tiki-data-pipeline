# ============================================
# TIKI DATA PIPELINE - Environment Variables
# ============================================
# Copy file này thành .env và điền các giá trị thực tế
# KHÔNG commit file .env vào git!

# ============================================
# PostgreSQL Configuration
# ============================================
# User và password cho PostgreSQL database
# Mặc định: postgres/postgres (chỉ dùng cho development)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_postgres_password_here

# Ví dụ mẫu:
# POSTGRES_USER=airflow_user
# POSTGRES_PASSWORD=MyStr0ng!P@ssw0rd2024

# ============================================
# Redis Configuration
# ============================================
# Password cho Redis (optional)
# Để trống nếu không muốn set password cho Redis 
# Nếu set password, Airflow sẽ tự động sử dụng trong connection string
REDIS_PASSWORD=

# Ví dụ mẫu:
# REDIS_PASSWORD=redis_secure_password_123
# REDIS_PASSWORD=MyRedis!Pass2024

# ============================================
# Airflow Configuration
# ============================================
# User ID cho Airflow (thường là UID của user hiện tại trên Linux)
# Trên Windows/Mac, có thể để mặc định 50000
AIRFLOW_UID=50000

# Username và password cho Airflow Web UI
# Login vào http://localhost:8080 với username/password này
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=your_airflow_web_password_here

# Ví dụ mẫu:
# _AIRFLOW_WWW_USER_USERNAME=admin
# _AIRFLOW_WWW_USER_PASSWORD=Admin@123456
# _AIRFLOW_WWW_USER_PASSWORD=MyAirflow!Pass2024

# ============================================
# Project Configuration
# ============================================
# Đường dẫn project (thường là . hoặc đường dẫn tuyệt đối)
AIRFLOW_PROJ_DIR=.
# Đường dẫn file .env (thường là .env)
ENV_FILE_PATH=.env
# Tên Docker image cho Airflow
AIRFLOW_IMAGE_NAME=tiki-airflow:3.1.2

# ============================================
# Optional: Python Dependencies
# ============================================
# Các packages Python bổ sung (đã có trong requirements.txt)
# Chỉ uncomment nếu muốn override hoặc thêm packages
# _PIP_ADDITIONAL_REQUIREMENTS=selenium>=4.0.0 beautifulsoup4>=4.12.0 requests>=2.31.0 lxml>=4.9.0 tqdm>=4.65.0 webdriver-manager>=4.0.0 redis>=5.0.0

# ============================================
# Development vs Production Examples
# ============================================

# === DEVELOPMENT (Local testing) ===
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# REDIS_PASSWORD=
# _AIRFLOW_WWW_USER_USERNAME=airflow
# _AIRFLOW_WWW_USER_PASSWORD=airflow

# === PRODUCTION (Secure) ===
# POSTGRES_USER=airflow_prod
# POSTGRES_PASSWORD=Str0ng!Pr0dP@ssw0rd2024
# REDIS_PASSWORD=Redis!Secure2024
# _AIRFLOW_WWW_USER_USERNAME=admin
# _AIRFLOW_WWW_USER_PASSWORD=Admin!Secure2024

# ============================================
# Security Best Practices
# ============================================
# 1. Sử dụng password mạnh (ít nhất 12 ký tự, có chữ hoa, chữ thường, số, ký tự đặc biệt)
# 2. Không commit file .env vào git (đã có trong .gitignore)
# 3. Rotate passwords định kỳ (mỗi 3-6 tháng)
# 4. Sử dụng password khác nhau cho mỗi môi trường (dev/staging/prod)
# 5. Cân nhắc dùng secrets manager (AWS Secrets Manager, HashiCorp Vault) cho production
